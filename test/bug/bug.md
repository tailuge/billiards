localUrl: http://localhost:8080/?ruletype=nineball&state=%28%27init%21%5B%22U%23U703784048557Z15%2FX054916163207963YU7631804347038269U0j8694Y796j1406U76j6658X094604%2F034j4Y568403244U9398768544197083UX020554296497721225U8216179013252258%2F06780850142240524U8214354515075684U067942366X399017U88X176382064819U0j64j5745573044U88X246Y9499207%2F0344418436Z88j6U8221290Y1541748%2FX0Z783941525034606%5D~shots%21%5BP0Q10759493708610535F-L3055670559406Z05H%22k%23*D1N1B-L07594936341047Z7Q%24FL423857j127593994HL61ZY625X38147k0Z14456634223461*D2N2Bq1W9F-3.46794X92086792HL65305X650405884QY764619499444962*D3N3Bq0W845FL879888Y731j85HL7662238478660583Q5418556j2588196*D4N4BL3XXXY9209Z96~y%210FL8692659735679626V1.6244X019645691TL7343571782Y2122k1359X55704Y682*D5N5B-L1329Y399X684357Q08860759437084198F-L9YY91034317017V2.67%26TL7327601909637451k012674345634877682*D6N6BL%24Q1265822798013687F-2.3810245990753174K2515013515949249kZ423953056j545*D7N7B-L025316456jZ02773k1W9F-L425534904X314jK6458439230918884Q1323547065258026*D7N0BJFL103847101j075714K1710Z807759Z497Q059Z926169872Z4%25PJF1.649442195892j4+4932214319705963Q43Z766167163849*D8N1BJF-L290617436170578+254Z54845523834k0988j3523273468*D9N2~active%210%29%5D~start%211774892219839~now%211774892385818~score%219~wholeGame%21true~v%211~%2Bs%21%28%27%2B1%21%27Anon%27~%2B2%21%27Opponent%27%29%29*%25%28%27type%21%27SCORE%27~clientId%21%27G_c377_260j0B~active%211%29%2CPD%27~%2Bname%21%27Anon%27~p1%21F~z%210%29~angle%21HV5.%264TJL1708860695362091Q13924050jY15723KV3.7204X094985962T-LL0.N~p2%210~b%21P%28%27type%21%27AIM%27~offset%21%28%27x%21Q~y%21-LT~pos%21%28%27x%21U%2CLV~power%21W01265825j12Y0X00Y11Z28j33k~y%21LqL0126582Z166401386Q+V2.1484X068Z3081T-L%22-L7204999923706055%2344873636960983276%24X6329Y40832X693%25~z%210%29~i%210%29%2C%2623999977Y1816%2Bplayer%2F%2C-L%01%2F%2B%26%25%24%23%22+qkjZYXWVUTQPNLKJHFDB*_


from codex:

## Additional Groundwork

### Browser replay findings

Added temporary replay logging in `src/controller/replay.ts` and ran a local browser build with the replay URL above.

Observed at shot 1 start:

```json
{
  "shotType": "AIM",
  "currentCueball": {
    "x": -0.7204999923706055,
    "y": 0.44873636960983276
  },
  "recordedAimPos": {
    "x": -0.7204999923706055,
    "y": 0.44873636960983276
  },
  "distance": 0
}
```

Observed after shot 1 settles, before shot 2 is applied:

```json
{
  "cueball": {
    "x": 0.6128004193305969,
    "y": 0.028110530227422714
  },
  "remainingShots": 20
}
```

Shot 2 recorded `aim.pos` is:

```json
{
  "x": 0.6128116250038147,
  "y": 0.02814456634223461
}
```

So the replay-side mismatch at the shot boundary is approximately:

- `dx ~= 0.0000112056732178`
- `dy ~= 0.0000340361148119`
- `d ~= 0.0000358332837422`

This is a strong signal that the break replay ends in a cueball position that is extremely close to, but not identical to, the recorded start position of shot 2.

### Important conclusion

This makes a broad “rogue input after hit” explanation less likely.

The most useful current working theory is:

1. shot 1 replay physics ends at a slightly different settled state than the original live shot 1
2. the break amplifies that tiny difference, so several balls visibly disagree from shot 2 onward

This still points more to a shot-boundary mismatch than to random instability.

### Test harness findings

A regression test was added at `test/bug/break_replay_regression.spec.ts`.

That test can reproduce a stable mismatch in the local Jest harness, but the harness currently lands at a much larger delta than the browser:

- direct fixed-step path: `0.03903063840011522`
- browser-like `1/60` frame loop in Jest: also `0.03903063840011522`

The browser build does **not** show this larger discrepancy. Therefore the Jest harness is currently not a faithful reproduction of the exact runtime path/state used by the local browser replay for this bug.

Do not treat the current Jest magnitude as evidence about the real replay bug. The useful part of the test is that it provides a deterministic local repro point for future investigation.

### Logging recommendation

After the experiment, the replay logging was reduced to a minimal form:

```ts
console.log("replay-shot-delta", {
  dx: aim.pos.x - this.container.table.cueball.pos.x,
  dy: aim.pos.y - this.container.table.cueball.pos.y,
  d: this.container.table.cueball.pos.distanceTo(aim.pos),
})
```

This is worth keeping while investigating because:

- it is concise
- it survives replay-only investigation
- `dx` and `dy` are more informative than only logging `distance`

### Stronger leads, weaker leads

Stronger current leads:

- `aim.pos` at the next shot boundary is not identical to the settled cueball position
- the discrepancy appears immediately after the break, which narrows the area a lot
- break physics is collision-sensitive, so a tiny boundary mismatch can fan out into larger visible differences

Weaker leads:

- random nondeterminism in `table.ts`
- rogue user input after hit
- `serialiseHit()` only sending ball 0 for spectator sync

That last point matters for watch/spectate paths, but the replay URL assembled by `Recorder.wholeGame()` is driven by `entries[0].state` plus recorded shot events, not by `HitEvent.tablejson.balls`.

### Best next steps if revisiting

If returning to this later, the best focused question is:

> Should `recorded AIM.pos for shot N` always equal `settled cueball.pos immediately before shot N`?

If temporary debug data is added again, the most useful low-cost thing to record per shot is **explicit cueball x/y at shot start** alongside the `AimEvent`, rather than serialising all moved balls.

That would directly test whether `aim.pos` is the wrong source of truth for replay shot starts.


## New Confirmed Findings

### The earlier large Jest discrepancy was a test harness artifact

An important hidden difference between browser replay and the local bug test was the **cushion model**.

Facts:

- `Table` defaults to `bounceHanBlend`
- `BrowserContainer` defaults to `mathavenAdapter` unless a `cushionModel` URL param is supplied
- the real browser replay for this bug was therefore using `mathavenAdapter`

The original local regression tests were unintentionally using `bounceHanBlend`, which produced a much larger deterministic mismatch (`~0.03903`).

After explicitly setting:

```ts
table.cushionModel = mathavenAdapter
```

in both the replay/container test path and the direct inner-loop test path, both local tests matched the browser-sized discrepancy:

- `d = 0.00003583328374224414`

This is a major cleanup of the investigation because it means the local regression now agrees with the browser on the magnitude of the mismatch.

### The local regression is now trustworthy for this bug

There are now two useful tests in `test/bug/break_replay_regression.spec.ts`:

1. replay/container path
2. direct inner-loop path

Both now reproduce the same tiny deterministic mismatch as the browser, using:

- identical init state
- identical first-shot aim
- `CollisionThrow`
- `mathavenAdapter`
- nine-ball table geometry with pockets

This means:

- the bug is reproducible locally without a browser
- it is not caused by wrong test geometry/constants/model selection anymore

### One-step-earlier cueball experiment

In the direct-inner-loop regression, looking one iteration earlier did **not** explain the mismatch in any strong way.

The last distinct cueball position before the final one was only microscopically closer than the final cueball position.

Interpretation:

- the mismatch is **not** simply “the cueball should have been sampled one final step earlier”
- the cueball is already essentially at the mismatched location when it stops moving

That weakens the idea that the bug is just an off-by-one stationary-step boundary issue.

### Current strongest state of the problem

What is now known:

1. the break replay discrepancy is deterministic
2. the local regression reproduces the same tiny discrepancy as the browser
3. the mismatch exists at the boundary between settled shot 1 and recorded shot 2
4. the cueball is already at the mismatched location when it stops, rather than becoming wrong only because other balls continue to roll

So the best remaining hypothesis is still:

- replayed shot 1 settles to a slightly different state than the original live shot 1
- shot 2 is then started from that tiny mismatch

This still points more to **missing or slightly wrong shot-boundary state** than to random physics instability.

Result is stable and reproducable in firefox, chrom, mobile and desktop.

* Could collision ordering differ during live vs replay?


Where to look first
I would bet heavily on Serialization/Precision Loss. Look closely at how the offset, angle, and pos arrays in your JSON/URL strings are being rounded or packed before being written to the replay log. If an AimEvent grabs a UI-rounded coordinate instead of the deep physics engine coordinate, the replay is doomed.

The Float Serialization Audit (Strongest Suspect)
Given that the mismatch is ~0.0000358 and appears immediately at the boundary of Shot 2, this screams precision loss during serialization.

JavaScript numbers are 64-bit floats. If the game records the AimEvent for Shot 2 by serializing the cueball's position to a string (for the URL) and then deserializing it, you might be losing microscopic precision.

from gemini:
you’ve successfully proved the simulation is stable, but it is simulating the wrong reality.If the replay is consistent across different browsers and devices, the physics engine itself is likely deterministic. The drift happens because the init state or the shots[0] data in that URL does not perfectly match the "ground truth" of the live game at the moment the user hit the break.Here is how I would pivot the investigation to find the "missing" initial state:1. The "Pre-Shot 1" Physics LeakIn many pool engines, balls are "placed" on the table and then a physics sub-step runs to resolve overlaps before the user can even aim.The Theory: When the live game started, the 1-9 balls had a microscopic overlap. The live engine resolved this before the break shot. If your init array records the positions before that resolution, but the live break happened after, your replay starts from a "stressed" state that the live game already passed.The Test: In your Jest harness, before applying the first shot, run table.tick() until isSettled is true. Compare those positions to your init array. If they move even $10^{-7}$, your init state is "dirty."2. The Deterministic "Ghost" StateCheck if your init array or Shot objects are missing a hidden physics variable that isn't pos or v.Sub-step Residuals: Does your engine maintain an accumulator for time? If the live game was at t = 0.0016 when the break hit, but your replay starts at t = 0, that phase shift in the integrator can cause the $\sim0.00003$ delta.Collision Graph/Sleep State: Are balls marked as sleeping in the init data? If the replay starts with all balls "awake" but the live game had them "asleep," the first frame of gravity/friction calculation might differ.3. The "Aim-to-Hit" GapThis is a high-probability culprit in recorded games.The Theory: The user aims (generating an AIM event at $T_0$), but the actual physics impulse (the HIT) happens at $T_1$.The Gap: If the cue ball (or any ball) was microscopically rolling or vibrating between the AIM event and the HIT event, the AIM.pos is no longer the true HIT.pos.The Evidence: You noted recorded AIM.pos vs settled cueball.pos. If your recorder saves the position when the "Aim" UI is active, but the user's browser processed one more physics tick before the "Hit" logic fired, the replay is using "stale" coordinates for the start of the vector.4. The "15-Ball" vs "9-Ball" PrecisionSince this is Nine-ball:Is the rack generated by a function that uses Math.random()? Even if the positions are saved in init, check if any part of the Table setup logic re-calculates offsets (like ball spacing/racking tightness) based on the rule type rather than strictly adhering to the init coordinates.Suggested Forensic ActionSince you can't see the live game, mutate your test harness to be a "Brute Force Optimizer":Take your current init state.Jitter the positions of the 1-ball and the Cueball by tiny increments ($10^{-6}$).Run the Shot 1 simulation for each jittered state.See if any specific starting coordinate results in a "Settled Shot 1" position that perfectly matches the shots[1].pos in the URL.If a tiny shift in the 1-ball's initial position fixes the discrepancy, you’ve confirmed that the init state recording is the bug—likely a race condition where the state is saved just before the balls are perfectly settled in the rack.

What you’ve convincingly proven
1. The simulation is deterministic
Same result across Firefox, Chrome, mobile, desktop
Same result in local harness (after fixing cushion model)

👉 That basically kills:

random FP nondeterminism
collision ordering differences (in the usual sense)

If ordering were unstable, you’d see divergence across runs/devices — you don’t.

2. The mismatch is tiny and occurs exactly at a shot boundary

You measured:

d ≈ 0.000035833

And critically:

it exists immediately between shot 1 settle and shot 2 start

👉 This is the most important fact in the whole investigation.

3. It is NOT:
❌ “one frame too early” sampling
❌ a big physics divergence
❌ test harness artifact (you fixed that)
4. The cue ball is already “wrong” when settled

This is subtle but huge:

the final resting position itself is slightly different

👉 So the error is not introduced after settling.
It originates during shot 1 simulation, not at the boundary check.

🚨 Where your current theory is slightly off

You’re leaning toward:

“missing or slightly wrong shot-boundary state”

That’s close — but I’d sharpen it:

The replay is simulating a different shot 1 than the live game did.

Not a different boundary — a different input into shot 1.

🔥 The strongest remaining culprit (by far)
👉 AIM ≠ actual HIT state

This is the one thing you haven’t disproven yet, and it perfectly fits:

The key idea:
T0: AIM event recorded (pos, angle, offset)
T1: Physics actually applies the shot impulse

If anything changes between T0 and T1, then:

Replay uses T0
Live game used T1

➡️ That produces exactly:

tiny deterministic offset (~1e-5)
visible divergence after break (chaotic amplification)
Why this fits your data perfectly
✔ Explains the tiny delta
Cue ball may move microscopically (spin decay, float noise, etc.)
Or even just one extra physics tick
✔ Explains why break amplifies it

Break shots are:

high energy
many simultaneous collisions
extremely sensitive to initial contact geometry

A 3e-5 shift is absolutely enough to:

change which ball is hit first
change collision order
cascade into visible differences
✔ Explains why everything else checks out
Deterministic? ✅
Reproducible? ✅
Stable across environments? ✅

Because you're consistently replaying the wrong initial condition.

🔍 Why I don’t think it’s serialization

You suspected:

precision loss in URL / float packing

But:

❌ That would affect:
shot 1 input directly
and you'd see mismatch at shot 1 start

You explicitly showed:

shot 1 start:
distance = 0

👉 So serialization is good enough for shot 1.

🔍 Why I don’t think it’s “dirty init”

Gemini’s idea:

rack overlap / pre-settle mismatch

Good thought — but:

Your mismatch is between shot 1 → shot 2
Not at shot 1 start

👉 If init were dirty, you'd see divergence immediately.

🧠 What’s actually happening (most likely)
The real sequence:
Live game:
frame N:
  player aims → AIM recorded (pos A)

frame N+1:
  physics tick happens
  cue ball moves slightly → pos B

frame N+2:
  HIT applied using pos B
Replay:
start from pos A
apply HIT immediately
Result:
shot 1 simulated from slightly wrong starting state
→ ends at slightly wrong final state
→ shot 2 mismatch appears
🧪 One definitive test (this will confirm it)

You already log:

dx = aim.pos.x - cueball.pos.x

Now add this at HIT time in live game:

console.log("HIT_vs_AIM", {
  aimX: aim.pos.x,
  aimY: aim.pos.y,
  hitX: cueball.pos.x,
  hitY: cueball.pos.y,
  dx: cueball.pos.x - aim.pos.x,
  dy: cueball.pos.y - aim.pos.y,
})
Expected result:

You will see something like:

dx ≈ 0.00001–0.00004
dy ≈ similar

👉 If yes → case closed.

🧩 If that’s confirmed, the fix is straightforward
✅ Correct fix (best)

Record actual cue ball position at HIT, not AIM:

HitEvent {
  cueballPos: actual physics position at impulse time
  angle
  offset
  power
}

Replay uses that instead of aim.pos.

⚠️ Alternative (weaker)

Force no physics tick between AIM and HIT:

freeze simulation during aiming
apply hit immediately

This is fragile and can break UX.

🧠 About your original question

Could collision ordering differ during live vs replay?

Answer: almost certainly no

Because:

Deterministic across platforms
Stable reproduction
Same inputs → same outputs (within your harness)

👉 The ordering is consistent — you're just feeding slightly different inputs.

🎯 Final takeaway

You’re not dealing with:

physics instability
collision ordering
rounding errors

You’re dealing with:

A timing gap between when AIM is recorded and when HIT is actually applied.

That gap is only ~1 frame — but that’s enough.

If you want to go one level deeper

I can help you:

instrument the exact AIM → HIT pipeline
trace event ordering vs physics ticks
or suggest a minimal patch that won’t break your replay format

This is very close to solved.

This is a rare bug and I'm onboard with the idea that after recording data there is some small nudge in state in live system before deterministic physics ensue. I have a way to log client side to server so if I could add a comparison that is non zero in error state i.e low volume I could add that 

Yeah — this is exactly the right approach now. You don’t want more logs, you want a tripwire that only fires when the bug condition actually happens.

Given everything you’ve proven, the cleanest signal is:

“Was the cueball position at HIT different from the AIM.pos we recorded?”

That’s your highest signal, lowest noise metric.

🎯 The exact check to add

At the moment the shot is actually executed (where you apply impulse):

const dx = cueball.pos.x - aim.pos.x
const dy = cueball.pos.y - aim.pos.y
const d = Math.sqrt(dx * dx + dy * dy)

Then gate it:

if (d > 1e-6) {
  report("aim_hit_mismatch", {
    dx,
    dy,
    d,
    aimX: aim.pos.x,
    aimY: aim.pos.y,
    hitX: cueball.pos.x,
    hitY: cueball.pos.y,
    shotIndex,
  })
}
🧠 Why 1e-6?
Your observed bug: ~3.5e-5
Float noise baseline: ~1e-8 to 1e-7

👉 So:

1e-6 = safely above noise
but still catches your bug reliably
🔥 Even better (very low volume, high signal)

Only log when it’s likely to matter:

if (d > 1e-6 && power > 4.5) {

Why:

Break shots amplify the issue massively
Normal shots might have tiny drift but no visible bug
🧪 Optional: confirm the replay bug directly

You can add a second check that correlates perfectly with your current finding:

At shot boundary (start of next shot):
const dx = nextAim.pos.x - cueball.pos.x
const dy = nextAim.pos.y - cueball.pos.y
const d = Math.sqrt(dx * dx + dy * dy)

if (d > 1e-6) {
  report("shot_boundary_mismatch", { dx, dy, d })
}

This gives you:

live-side evidence of the same issue you saw in replay
without needing replay at all
📊 What you’ll learn from this

After a bit of traffic, you’ll see patterns like:

Case A (most likely)
aim_hit_mismatch: d ≈ 0.00002–0.00005

👉 Confirms:

AIM was recorded too early

Case B (less likely but interesting)
aim_hit_mismatch: d = 0
shot_boundary_mismatch: d > 0

👉 That would imply:

something changes after the hit (much rarer)

Case C (unlikely)
both always zero

👉 Then we revisit serialization or hidden state

🧩 Bonus: catch the root cause precisely

If you want to go one level deeper (still low volume):

Log frame/tick timing
report("aim_timing", {
  aimFrame,
  hitFrame,
  frameDelta: hitFrame - aimFrame,
})

If you see:

frameDelta = 1 or 2

👉 That’s your smoking gun.

🚀 Minimal production-safe version

If you want something super lean:

const d = cueball.pos.distanceTo(aim.pos)

if (d > 1e-6) {
  report("aim_drift", d)
}



# Forward

I have a way to record live game limited logging so I want a tripwire comparison that I can console.warn on if there is a mismatch. 

This can be at various points in the code but I presume at the start of the main sim cycle somehow looking ta what was recorded vs current sim start state is a way.
